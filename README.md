# 🚀 NESTJS-BASIC: Grundlagen der Architektur

[![NestJS Logo](https://nestjs.com/img/logo_text.svg)](https://nestjs.com/)
---

## 🌟 Über dieses Projekt

Dieses Repository dient als meine **Grundlagen-Sandbox** zum Erlernen und Demonstrieren der Kernprinzipien des **NestJS**-Frameworks und seiner TypeScript-orientierten Architektur.

Das Projekt zeigt eine minimale, aber voll funktionsfähige NestJS-Struktur, einschließlich der Schlüsselkomponenten: **Module, Controller und Provider (Services)**, sowie den Mechanismus der **Dependency Injection (DI)**.

### 🎯 Wichtige erlernte Konzepte

* **TypeScript Decorators:** Die Verwendung von `@Controller()`, `@Injectable()`, `@Get()`, `@Module()`.
* **Dependency Injection (DI):** Die korrekte Injektion von `AppService` in den `AppController` über den Konstruktor.
* **Modularität:** Die Erstellung eines Root-Moduls (`AppModule`).
* **Bootstrap:** Der Start des HTTP-Servers mithilfe von `NestFactory`.

---

## 🛠️ Technologien und Stack

| Tool | Beschreibung |
| :--- | :--- |
| **NestJS** | Das primäre Node.js Framework. |
| **TypeScript** | Die Programmiersprache, basierend auf starker Typisierung. |
| **Node.js** | Die Laufzeitumgebung (mindestens v18). |
| **npm/yarn** | Der Paketmanager. |

---

## ⚙️ Installation und Start

Um dieses Projekt lokal auszuführen, befolgen Sie bitte die folgenden Schritte.

### 1. Repository klonen

```bash
git clone https://github.com/kestenbaum/nestjs-basic.git
```
